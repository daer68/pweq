---
name: Bug report
about: Something doesn't work as expected
labels: bug
---

**What happened**

**What you expected**

**Steps to reproduce**

1.
2.

**Environment**

- Distribution:
- pweq version (`pweq --version`):
- PipeWire version (`pipewire --version`):
- WirePlumber version (`wireplumber --version`):
- Desktop / compositor:

**`pweq status`**

```text

```

**Logs** (`journalctl --user -u pweq -b`)

```text

```
